Standard Workflows
------------------

This directory contains standard workflows. These should *never* be changed. If
you need to change a workflow then copy it to ../custom where site specific
workflows are stored. You can then do whatever changes you want to the custom
workflow.
